---
phase: 06-testing-plugin-hub-submission
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "README describes the plugin's purpose and blocked travel method counts"
    - "README has build-from-source instructions (gradlew build, gradlew run)"
    - "README describes architecture (event flow, blocker services, boundary checker)"
    - "Plugin Hub artifacts are verified correct (LICENSE, runelite-plugin.properties, icon.png dimensions)"
  artifacts:
    - path: "README.md"
      provides: "Developer-focused documentation for Plugin Hub listing"
      min_lines: 40
  key_links:
    - from: "README.md"
      to: "runelite-plugin.properties"
      via: "Plugin identity consistency"
      pattern: "Varlamore UIM"
---

<objective>
Rewrite the placeholder README.md with developer-focused documentation and verify all Plugin Hub submission artifacts are correct.

Purpose: Prepare the repository for Plugin Hub submission (INFRA-01..04) with proper documentation that describes the plugin, how to build it, and its architecture.

Output: Complete README.md, verified Plugin Hub artifacts
</objective>

<execution_context>
@C:/Users/Sallis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sallis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-plugin-hub-submission/06-CONTEXT.md

@README.md
@runelite-plugin.properties
@LICENSE
@build.gradle
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md</name>
  <files>README.md</files>
  <action>
Replace the placeholder README.md ("# Example / An example greeter plugin") with developer-focused documentation.

**Per locked decisions:**
- Developer-focused — architecture, building from source, contributing
- Summary counts ("31 spells, 80+ items, 3 NPC docks") — no exhaustive lists
- No screenshots or visuals — text-only
- Assume audience knows what area-locked accounts are — no intro explanation
- Plugin name: "Varlamore UIM"
- Description tone: functional

**README structure (Claude's discretion on ordering):**

1. **Title + one-line description**: "# Varlamore UIM" + "Blocks all teleports and transport that leave the Varlamore region for area-locked accounts."

2. **Features section**: Summary with counts. Group by category:
   - Spell blocking: 31 spellbook teleports + Home Teleport across Standard, Ancient, Lunar, and Arceuus
   - Item blocking: 80+ item variants (jewelry, teleport tablets, quest items, achievement diary gear)
   - Per-destination: Ring of Dueling (allows Fortis Colosseum), Hunter/Max cape (allows Hunter Guild), House tablet (configurable)
   - NPC transport: Charter ship crew hidden at 3 Varlamore docks with Mysterious Old Man stand-ins, Primio quetzal blocked
   - Minigame tab: All 17 minigame teleports blocked
   - Unlock gate: Dizana's Quiver ownership restores charter ship access

3. **Building from source**:
   ```
   ./gradlew build    # Compile + JAR
   ./gradlew run      # Launch RuneLite with plugin (developer mode)
   ./gradlew test     # Run unit tests
   ```
   Requires Java 11+.

4. **Architecture** (2-3 paragraphs):
   - Event-driven: `onMenuOptionClicked` dispatches to SpellTeleportBlocker, ItemTeleportBlocker, NpcTransportBlocker in sequence
   - Boundary detection: BoundaryChecker loads chunk IDs from varlamore_regions.json, O(1) HashSet lookup
   - Config toggles: Each blocking category has an independent config toggle in the Restrictions section
   - NPC replacement: Charter ship NPCs hidden via RenderCallback, Mysterious Old Man stand-ins spawned via RuneLiteObject

5. **Known limitations**:
   - Region IDs are calculated from coordinate ranges — some may include ocean tiles (pending in-game boundary walk)
   - Home Teleport blocked unconditionally (all current destinations are outside Varlamore)
   - House tablet defaults to blocked (enable config toggle if POH is in Aldarin)
   - Quetzal whitelist is empty (pending in-game data collection)

6. **Contributing**: Standard "open an issue" / "submit a PR" text. Mention BSD 2-Clause license.

7. **License**: One-line "BSD 2-Clause. See LICENSE."

Keep it concise — aim for ~60-80 lines total. No emojis. No badges.
  </action>
  <verify>Read the file and confirm it has all required sections, accurate counts, and no placeholder text.</verify>
  <done>README.md is a complete developer-focused document with plugin description, feature counts, build instructions, architecture overview, known limitations, and contributing guidelines.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Plugin Hub submission artifacts</name>
  <files>README.md</files>
  <action>
Verify the following Plugin Hub required artifacts exist and are correct. Do NOT modify them — only verify and log findings:

1. **LICENSE** — Confirm file exists and contains "BSD 2-Clause License". (Expected: already correct.)

2. **runelite-plugin.properties** — Confirm:
   - `displayName=Varlamore UIM`
   - `plugins=com.varlamoreuim.VarlamoreUimPlugin` (matches actual class path)
   - `author=Sallis`
   - Has description and tags

3. **icon.png** — Confirm file exists at repository root. Check dimensions are within Plugin Hub limit (max 48x72 px). The file is expected to be 32x32 based on research. Use `identify icon.png` (ImageMagick) or `file icon.png` to check.

4. **build.gradle** — Confirm `group = 'com.varlamoreuim'` and Java 11 target (`options.release.set(11)`).

5. **Class naming** — Confirm the main plugin class exists at `src/main/java/com/varlamoreuim/VarlamoreUimPlugin.java` and has `@PluginDescriptor(name = "Varlamore UIM")`.

If any artifact is missing or incorrect, fix it. If all pass, note the verification results in the summary.

**Plugin Hub submission itself** (forking runelite/plugin-hub, creating manifest PR) is a checkpoint:human-action that will be done manually by the user after all automated work is complete. Do NOT attempt the submission PR — just verify readiness.
  </action>
  <verify>All 5 artifacts verified as correct. List each with status.</verify>
  <done>All Plugin Hub submission artifacts (LICENSE, runelite-plugin.properties, icon.png, build.gradle, plugin class) are verified correct and ready for submission.</done>
</task>

</tasks>

<verification>
- README.md is a complete, non-placeholder document with all required sections
- All Plugin Hub artifacts verified as present and correctly configured
- `./gradlew build` succeeds (confirms no structural issues)
</verification>

<success_criteria>
- README.md contains plugin description, feature counts, build instructions, architecture, limitations, and contributing
- LICENSE is BSD 2-Clause
- runelite-plugin.properties matches actual plugin class
- icon.png exists within 48x72 px dimension limit
- Repository is ready for Plugin Hub submission PR
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-plugin-hub-submission/06-03-SUMMARY.md`
</output>
