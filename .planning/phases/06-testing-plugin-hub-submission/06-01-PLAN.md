---
phase: 06-testing-plugin-hub-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - src/test/java/com/varlamoreuim/BoundaryCheckerTest.java
  - src/test/java/com/varlamoreuim/teleport/SpellTeleportBlockerTest.java
autonomous: true
requirements:
  - BNDRY-01
  - BNDRY-02
  - BNDRY-03
  - SPELL-01
  - SPELL-02
  - SPELL-03
  - SPELL-04
  - SPELL-05
  - SPELL-06

must_haves:
  truths:
    - "BoundaryChecker loads regions from JSON and correctly identifies Varlamore vs non-Varlamore region IDs"
    - "BoundaryChecker quetzal whitelist mechanism works (even with empty set)"
    - "SpellTeleportBlocker consumes events for all 31 blocked spell names across all 4 spellbooks"
    - "SpellTeleportBlocker consumes Home Teleport events when destination is outside Varlamore"
    - "SpellTeleportBlocker ignores non-spellbook CC_OP events (equipment panel clicks)"
  artifacts:
    - path: "build.gradle"
      provides: "Mockito + guice-testlib test dependencies"
      contains: "mockito-core"
    - path: "src/test/java/com/varlamoreuim/BoundaryCheckerTest.java"
      provides: "Region loading and boundary checks"
      min_lines: 30
    - path: "src/test/java/com/varlamoreuim/teleport/SpellTeleportBlockerTest.java"
      provides: "All 31 spell + Home Teleport blocking tests"
      min_lines: 80
  key_links:
    - from: "SpellTeleportBlockerTest.java"
      to: "SpellTeleportBlocker.handleMenuClick()"
      via: "MenuOptionClicked constructed from mocked MenuEntry"
      pattern: "new MenuOptionClicked"
    - from: "BoundaryCheckerTest.java"
      to: "BoundaryChecker.loadRegions()"
      via: "Direct instantiation + classpath resource loading"
      pattern: "loadRegions"
---

<objective>
Add test dependencies (Mockito, guice-testlib) to build.gradle and write unit tests for BoundaryChecker and SpellTeleportBlocker services.

Purpose: Validate the boundary detection system (BNDRY-01..03) and all spell teleport blocking (SPELL-01..06) with automated JUnit 4 tests, ensuring these services correctly reject blocked actions.

Output: build.gradle with test deps, BoundaryCheckerTest.java, SpellTeleportBlockerTest.java
</objective>

<execution_context>
@C:/Users/Sallis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sallis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-plugin-hub-submission/06-RESEARCH.md

@build.gradle
@src/main/java/com/varlamoreuim/BoundaryChecker.java
@src/main/java/com/varlamoreuim/teleport/SpellTeleportBlocker.java
@src/main/resources/varlamore_regions.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test dependencies to build.gradle</name>
  <files>build.gradle</files>
  <action>
Add two test dependencies to the `dependencies` block in build.gradle, after the existing `testImplementation 'junit:junit:4.12'` line:

```gradle
testImplementation 'org.mockito:mockito-core:5.17.0'
testImplementation(group: 'com.google.inject.extensions', name: 'guice-testlib', version: '4.1.0') {
    exclude group: 'com.google.inject', module: 'guice'
}
```

The guice exclusion prevents version conflicts with RuneLite's bundled Guice 4.1.0. These are testImplementation-only, not shipped in the plugin JAR.
  </action>
  <verify>Run `./gradlew dependencies --configuration testCompileClasspath` and confirm mockito-core and guice-testlib appear in the output.</verify>
  <done>build.gradle has mockito-core 5.17.0 and guice-testlib 4.1.0 in testImplementation scope; project compiles without dependency conflicts.</done>
</task>

<task type="auto">
  <name>Task 2: Write BoundaryCheckerTest</name>
  <files>src/test/java/com/varlamoreuim/BoundaryCheckerTest.java</files>
  <action>
Create `src/test/java/com/varlamoreuim/BoundaryCheckerTest.java`. BoundaryChecker is a plain object with no mocking needed — instantiate directly and call loadRegions().

Tests to write (block-only — verify the system works, not edge cases):
1. `regionDataLoads()` — After loadRegions(), isLoaded() returns true and getRegionCount() > 0.
2. `knownVarlamoreRegionIsInside()` — isInVarlamore(6704) returns true (Civitas illa Fortis from varlamore_regions.json).
3. `knownNonVarlamoreRegionIsOutside()` — isInVarlamore(12853) returns false (Varrock region).
4. `isInVarlamoreByWorldPoint()` — isInVarlamore(new WorldPoint(1681, 3133, 0)) returns true (Civitas illa Fortis coordinates, region 6704).
5. `quetzalWhitelistMechanismWorks()` — isQuetzalWhitelisted(99999) returns false (verifies the method works with empty set; actual IDs pending in-game collection).

Use JUnit 4 `@Test`, `@Before`, `assertTrue`, `assertFalse`. No Mockito needed. Import `net.runelite.api.coords.WorldPoint` for the WorldPoint test.
  </action>
  <verify>Run `./gradlew test --tests "com.varlamoreuim.BoundaryCheckerTest"` — all 5 tests pass.</verify>
  <done>BoundaryCheckerTest validates region loading, in/out boundary detection by region ID and WorldPoint, and quetzal whitelist mechanism.</done>
</task>

<task type="auto">
  <name>Task 3: Write SpellTeleportBlockerTest</name>
  <files>src/test/java/com/varlamoreuim/teleport/SpellTeleportBlockerTest.java</files>
  <action>
Create `src/test/java/com/varlamoreuim/teleport/SpellTeleportBlockerTest.java`. Uses `@RunWith(MockitoJUnitRunner.class)`.

**Setup:**
- `@Mock ChatMessageManager chatMessageManager;`
- `@Mock BoundaryChecker boundaryChecker;`
- `SpellTeleportBlocker blocker = new SpellTeleportBlocker();` in `@Before`
- Default: `when(boundaryChecker.isInVarlamore(any(WorldPoint.class))).thenReturn(false);` (destinations outside Varlamore)

**Helper method:**
```java
private MenuOptionClicked makeSpellClick(String spellName, int widgetGroupId)
{
    MenuEntry entry = mock(MenuEntry.class);
    when(entry.getMenuAction()).thenReturn(MenuAction.CC_OP);
    when(entry.getOption()).thenReturn("Cast");
    when(entry.getTarget()).thenReturn(spellName);
    Widget widget = mock(Widget.class);
    when(widget.getId()).thenReturn(widgetGroupId << 16);
    when(entry.getWidget()).thenReturn(widget);
    return new MenuOptionClicked(entry);
}
```

IMPORTANT: Check whether MenuEntry uses `getTarget()` or `getMenuTarget()` — the research notes a possible getter mismatch. `MenuOptionClicked.getMenuTarget()` delegates to the entry's target getter. Check the actual RuneLite API: `MenuEntry` has `getTarget()` (and `setTarget()`), while `MenuOptionClicked.getMenuTarget()` calls `menuEntry.getTarget()`. Similarly `MenuEntry.getOption()` maps to `MenuOptionClicked.getMenuOption()`. So mock `entry.getOption()` and `entry.getTarget()`.

**Tests to write (all assert `event.isConsumed() == true` and `handled == true`):**

1. `standardSpellsAreBlocked()` — Test representative spells from each spellbook:
   - "Varrock Teleport" (Standard, widget 218)
   - "Paddewwa Teleport" (Ancient, widget 219)
   - "Moonclan Teleport" (Lunar, widget 430)
   - "Cemetery Teleport" (Arceuus, widget 218)
   Loop through all 4, assert each is consumed.

2. `homeTeleportIsBlocked()` — Spell name "Home Teleport" on widget 218, boundaryChecker returns false for destination. Assert consumed.

3. `nonSpellbookWidgetIsIgnored()` — CC_OP event with widget group ID 149 (equipment panel). Should return false and NOT consume.

4. `cancelOptionIsIgnored()` — CC_OP event with option "Cancel" on widget 218. Should return false.

5. `allBlockedSpellNamesAreRejected()` — Parametric-style: iterate all 31 spell names from the known set, call handleMenuClick for each, assert all are consumed. Use a local array of all 31 spell names (copy from SpellTeleportBlocker source). This is the comprehensive coverage test.

Each test calls `blocker.handleMenuClick(event, chatMessageManager, boundaryChecker)`.
  </action>
  <verify>Run `./gradlew test --tests "com.varlamoreuim.teleport.SpellTeleportBlockerTest"` — all tests pass.</verify>
  <done>SpellTeleportBlockerTest validates all 31 blocked spells are consumed, Home Teleport is blocked, non-spellbook events are ignored, and Cancel/Examine options pass through.</done>
</task>

</tasks>

<verification>
Run `./gradlew test` — all tests in BoundaryCheckerTest and SpellTeleportBlockerTest pass. No compilation errors.
</verification>

<success_criteria>
- build.gradle includes mockito-core 5.17.0 and guice-testlib 4.1.0 as testImplementation
- BoundaryCheckerTest has 5 passing tests covering region loading, boundary detection, and quetzal whitelist
- SpellTeleportBlockerTest has 5 passing tests covering all 31 spell names, Home Teleport, and edge cases
- `./gradlew test` passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-plugin-hub-submission/06-01-SUMMARY.md`
</output>
