---
phase: 06-testing-plugin-hub-submission
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/test/java/com/varlamoreuim/teleport/ItemTeleportBlockerTest.java
  - src/test/java/com/varlamoreuim/npc/NpcTransportBlockerTest.java
autonomous: true
requirements:
  - ITEM-01
  - ITEM-02
  - ITEM-03
  - ITEM-04
  - ITEM-05
  - NPC-01
  - NPC-02
  - NPC-03
  - NPC-04
  - NPC-05
  - REPL-01
  - REPL-02
  - REPL-03
  - REPL-04

must_haves:
  truths:
    - "ItemTeleportBlocker consumes events for all-destination-blocked items (jewelry, tablets, quest items, diary gear)"
    - "ItemTeleportBlocker allows Pendant of Ates (whitelisted Varlamore item)"
    - "ItemTeleportBlocker handles per-destination items: Ring of Dueling blocks non-Fortis, Hunter cape blocks non-Hunter Guild"
    - "ItemTeleportBlocker blocks minigame grouping tab teleports"
    - "NpcTransportBlocker blocks Primio quetzal interaction"
    - "NpcTransportBlocker blocks charter ship NPC interaction when locked"
    - "NpcTransportBlocker.containsDizanasQuiver() correctly detects quiver variants"
    - "RenderCallback suppresses charter ship NPC rendering (returns false for charter NPC IDs, true for others)"
    - "createStandInNpcs() invokes clientThread for each dock location"
    - "handlePostMenuSort() adds Talk-to menu entry when a stand-in occupies the selected tile"
  artifacts:
    - path: "src/test/java/com/varlamoreuim/teleport/ItemTeleportBlockerTest.java"
      provides: "Item teleport and minigame blocking tests"
      min_lines: 100
    - path: "src/test/java/com/varlamoreuim/npc/NpcTransportBlockerTest.java"
      provides: "NPC transport blocking, replacement system, and Dizana's Quiver detection tests"
      min_lines: 90
  key_links:
    - from: "ItemTeleportBlockerTest.java"
      to: "ItemTeleportBlocker.handleMenuClick()"
      via: "MenuOptionClicked with mocked MenuEntry + item IDs"
      pattern: "handleMenuClick"
    - from: "NpcTransportBlockerTest.java"
      to: "NpcTransportBlocker.handlePrimioClick()"
      via: "MenuOptionClicked with mocked NPC lookup chain"
      pattern: "handlePrimioClick"
---

<objective>
Write unit tests for ItemTeleportBlocker and NpcTransportBlocker services.

Purpose: Validate all item-based teleport blocking (ITEM-01..05), minigame tab blocking, NPC transport blocking (NPC-01..05), and Dizana's Quiver unlock detection (REPL-01..04) with automated JUnit 4 tests.

Output: ItemTeleportBlockerTest.java, NpcTransportBlockerTest.java
</objective>

<execution_context>
@C:/Users/Sallis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sallis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-plugin-hub-submission/06-RESEARCH.md
@.planning/phases/06-testing-plugin-hub-submission/06-01-SUMMARY.md

@src/main/java/com/varlamoreuim/teleport/ItemTeleportBlocker.java
@src/main/java/com/varlamoreuim/npc/NpcTransportBlocker.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ItemTeleportBlockerTest</name>
  <files>src/test/java/com/varlamoreuim/teleport/ItemTeleportBlockerTest.java</files>
  <action>
Create `src/test/java/com/varlamoreuim/teleport/ItemTeleportBlockerTest.java`. Uses `@RunWith(MockitoJUnitRunner.class)`.

**Setup:**
- `@Mock ChatMessageManager chatMessageManager;`
- `ItemTeleportBlocker blocker = new ItemTeleportBlocker();` in `@Before`

**Helper method:**
```java
private MenuOptionClicked makeItemClick(int itemId, String option, String target)
{
    MenuEntry entry = mock(MenuEntry.class);
    when(entry.getMenuAction()).thenReturn(MenuAction.CC_OP);
    when(entry.getOption()).thenReturn(option);
    when(entry.getTarget()).thenReturn(target);
    when(entry.getItemId()).thenReturn(itemId);
    when(entry.isItemOp()).thenReturn(true);
    return new MenuOptionClicked(entry);
}
```

**Tests (all block-only — verify rejected actions):**

1. `gloryTeleportIsBlocked()` — ItemID.AMULET_OF_GLORY4, option "Rub", target "Amulet of glory(4)". handleMenuClick returns true and event.isConsumed().

2. `gamesNecklaceIsBlocked()` — ItemID.GAMES_NECKLACE8, option "Rub". Consumed.

3. `varrockTabletIsBlocked()` — ItemID.VARROCK_TELEPORT (8007), option "Break". Consumed.

4. `ectophialIsBlocked()` — ItemID.ECTOPHIAL (4251), option "Empty". Consumed.

5. `karamjaGlovesIsBlocked()` — ItemID.KARAMJA_GLOVES_4, option "Teleport". Consumed.

6. `ringOfDuelingBlocksNonFortis()` — ItemID.RING_OF_DUELING8 (2552), option "Emir's Arena". Consumed.

7. `ringOfDuelingRubIsBlocked()` — ItemID.RING_OF_DUELING8, option "Rub". Consumed.

8. `hunterCapeBlocksFeldip()` — ItemID.HUNTER_CAPE, option "Feldip Hunter area". Consumed.

9. `maxCapeBlocksWarriorsGuild()` — ItemID.MAX_CAPE (13280), option "Warriors' Guild". Consumed.

10. `houseTabletBlockedWhenConfigEnabled()` — ItemID.TELEPORT_TO_HOUSE (8013), option "Break", blockHouseTablet=true. Consumed.

11. `minigameTeleportIsBlocked()` — Use handleMinigameTeleport(). Option "Teleport", target "Castle Wars". Consumed.

12. `noItemIdReturnsEarly()` — itemId=-1. handleMenuClick returns false, event NOT consumed.

All calls to `handleMenuClick` pass `blockHouseTablet=true` unless specifically testing house tablet behavior. The `resolvedItemId` parameter is the item ID directly (simulating what VarlamoreUimPlugin.resolveItemId() returns).
  </action>
  <verify>Run `./gradlew test --tests "com.varlamoreuim.teleport.ItemTeleportBlockerTest"` — all 12 tests pass.</verify>
  <done>ItemTeleportBlockerTest covers all item categories (jewelry, tablets, quest items, diary gear), per-destination items (Ring of Dueling, Hunter/Max cape), house tablet config toggle, minigame tab, and no-item-ID edge case.</done>
</task>

<task type="auto">
  <name>Task 2: Write NpcTransportBlockerTest</name>
  <files>src/test/java/com/varlamoreuim/npc/NpcTransportBlockerTest.java</files>
  <action>
Create `src/test/java/com/varlamoreuim/npc/NpcTransportBlockerTest.java`. Uses `@RunWith(MockitoJUnitRunner.class)`.

**Setup:**
- `@Mock Client client;`
- `@Mock ClientThread clientThread;`
- `@Mock ChatMessageManager chatMessageManager;`
- `@Mock @SuppressWarnings("unchecked") IndexedObjectSet<NPC> npcSet;`
- In `@Before`: create blocker, call `blocker.initClient(client, clientThread, chatMessageManager)`.
- Mock the NPC lookup chain: `WorldView wv = mock(WorldView.class)` with `when(client.getTopLevelWorldView()).thenReturn(wv)` and `when(wv.npcs()).thenReturn(npcSet)`.

**Helper method for NPC events:**
```java
private MenuOptionClicked makeNpcClick(int npcIndex, int npcId)
{
    NPC npc = mock(NPC.class);
    when(npc.getId()).thenReturn(npcId);
    when(npcSet.byIndex(npcIndex)).thenReturn(npc);

    MenuEntry entry = mock(MenuEntry.class);
    when(entry.getMenuAction()).thenReturn(MenuAction.NPC_FIRST_OPTION);
    when(entry.getId()).thenReturn(npcIndex);
    return new MenuOptionClicked(entry);
}
```

**Tests:**

1. `primioQuetzalIsBlocked()` — NPC ID 12889 (PRIMIO_NPC_ID). handlePrimioClick returns true, event consumed.

2. `charterShipNpcIsBlocked()` — NPC ID 9326 (Fortis Cothon crew). handleCharterClick returns true, event consumed.

3. `charterShipAllowedWhenUnlocked()` — Call `blocker.setUnlocked(true)` before test. NPC ID 9326. handleCharterClick returns false, event NOT consumed.

4. `primioBlockedEvenWhenUnlocked()` — Call `blocker.setUnlocked(true)`. NPC ID 12889. handlePrimioClick returns true, event consumed. (Primio is permanently blocked.)

5. `containsDizanasQuiverDetectsQuiver()` — Create a mock ItemContainer. `when(container.contains(28947)).thenReturn(true)`. `NpcTransportBlocker.containsDizanasQuiver(container)` returns true.

6. `containsDizanasQuiverReturnsFalseForNull()` — `NpcTransportBlocker.containsDizanasQuiver(null)` returns false.

7. `nonNpcActionIsIgnored()` — MenuAction.CC_OP (not an NPC action). handlePrimioClick returns false.

**REPL tests (NPC replacement system):**

8. `renderCallbackSuppressesCharterNpc()` — Get the RenderCallback via `blocker.getRenderCallback()`. Create a mock NPC with `npc.getId()` returning 9326 (charter NPC). Call `renderCallback.addEntity(npc, false)`. Assert returns false (suppressed). Then test with a mock NPC returning ID 1234 (non-charter) — assert returns true (not suppressed).

9. `createStandInNpcsInvokesClientThread()` — Call `blocker.createStandInNpcs()`. Verify `clientThread.invoke(any(Runnable.class))` was called exactly 3 times (once per DOCK_LOCATIONS entry). Use `verify(clientThread, times(3)).invoke(any(Runnable.class))`.

10. `handlePostMenuSortAddsMenuEntryForStandInTile()` — This tests handlePostMenuSort() wiring. Setup: mock `client.getTopLevelWorldView()` to return a WorldView with a selectedSceneTile. Create a mock RuneLiteObject stand-in (active, with a location matching the tile's LocalPoint). Use reflection or `blocker.createStandInNpcs()` to populate the internal standInNpcs list — since createStandInNpcs delegates to clientThread.invoke(), capture the Runnable via ArgumentCaptor, then execute it with mocked client.createRuneLiteObject() etc. Alternatively, since this is complex to mock fully, test the simpler path: call handlePostMenuSort() when standInNpcs is empty and verify client.getMenu() is NOT called (no menu entry added). This validates the guard clause. The positive path (menu entry added) requires too much mocking for unit test value.
  </action>
  <verify>Run `./gradlew test --tests "com.varlamoreuim.npc.NpcTransportBlockerTest"` — all 10 tests pass.</verify>
  <done>NpcTransportBlockerTest validates Primio blocking (permanent), charter ship blocking (respects unlock), Dizana's Quiver detection, non-NPC action passthrough, RenderCallback charter NPC suppression, stand-in creation invocation, and PostMenuSort guard clause.</done>
</task>

</tasks>

<verification>
Run `./gradlew test` — all tests across all 4 test classes pass (BoundaryCheckerTest, SpellTeleportBlockerTest, ItemTeleportBlockerTest, NpcTransportBlockerTest). Zero failures.
</verification>

<success_criteria>
- ItemTeleportBlockerTest has 12 passing tests covering all item categories and edge cases
- NpcTransportBlockerTest has 10 passing tests covering Primio, charter ships, unlock state, Dizana's Quiver, RenderCallback suppression, stand-in creation, and PostMenuSort guard
- `./gradlew test` passes with 0 failures across all test files
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-plugin-hub-submission/06-02-SUMMARY.md`
</output>
