---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - settings.gradle
  - runelite-plugin.properties
  - LICENSE
  - src/main/java/com/varlamoreuim/BoundaryChecker.java
  - src/main/java/com/varlamoreuim/VarlamoreUimPlugin.java
  - src/main/java/com/varlamoreuim/VarlamoreUimConfig.java
  - src/main/resources/varlamore_regions.json
  - src/test/java/com/varlamoreuim/VarlamoreUimPluginTest.java
  - src/test/resources/logback-test.xml
autonomous: true

must_haves:
  truths:
    - "Plugin project has correct identity (com.varlamoreuim package, Varlamore UIM name)"
    - "BoundaryChecker can determine if a region ID is inside or outside Varlamore in O(1) time"
    - "Region data loads from JSON resource file via getResourceAsStream()"
    - "Plugin compiles with ./gradlew build without errors"
  artifacts:
    - path: "src/main/java/com/varlamoreuim/BoundaryChecker.java"
      provides: "O(1) region boundary lookup service"
      exports: ["isInVarlamore", "isQuetzalWhitelisted", "loadRegions"]
    - path: "src/main/resources/varlamore_regions.json"
      provides: "Varlamore region ID data"
      contains: "regions"
    - path: "build.gradle"
      provides: "Build configuration with correct group"
      contains: "com.varlamoreuim"
    - path: "runelite-plugin.properties"
      provides: "Plugin Hub metadata"
      contains: "Varlamore UIM"
    - path: "LICENSE"
      provides: "BSD-2-Clause license"
      contains: "BSD-2-Clause"
    - path: "settings.gradle"
      provides: "Project name"
      contains: "varlamore-uim"
  key_links:
    - from: "src/main/java/com/varlamoreuim/BoundaryChecker.java"
      to: "src/main/resources/varlamore_regions.json"
      via: "getResourceAsStream(\"/varlamore_regions.json\")"
      pattern: "getResourceAsStream.*varlamore_regions"
---

<objective>
Transform the example-plugin template into the Varlamore UIM plugin project with correct identity, licensing, and a working boundary detection service.

Purpose: Establish the project foundation that all future phases build on -- correct package structure, Plugin Hub-ready metadata, and the core boundary checker that determines if any location is inside or outside Varlamore.

Output: Compilable plugin project with BoundaryChecker service and region data.
</objective>

<execution_context>
@C:/Users/Sallis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sallis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@src/main/java/com/example/ExamplePlugin.java
@src/main/java/com/example/ExampleConfig.java
@src/test/java/com/example/ExamplePluginTest.java
@build.gradle
@settings.gradle
@runelite-plugin.properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename project from example-plugin template to Varlamore UIM</name>
  <files>
    build.gradle
    settings.gradle
    runelite-plugin.properties
    LICENSE
    src/main/java/com/varlamoreuim/VarlamoreUimPlugin.java
    src/main/java/com/varlamoreuim/VarlamoreUimConfig.java
    src/test/java/com/varlamoreuim/VarlamoreUimPluginTest.java
    src/test/resources/logback-test.xml
  </files>
  <action>
    Rename the entire project from the example-plugin template to Varlamore UIM identity. This is a wholesale replacement, not incremental editing.

    1. DELETE the old com/example/ directories entirely (src/main/java/com/example/ and src/test/java/com/example/).

    2. Create `src/main/java/com/varlamoreuim/VarlamoreUimPlugin.java`:
       - Package: `com.varlamoreuim`
       - @PluginDescriptor: name = "Varlamore UIM", description = "Varlamore area-lock restrictions for Ultimate Ironman accounts", tags = {"varlamore", "uim", "ironman", "region", "restriction"}
       - Extends Plugin, @Slf4j annotation
       - @Inject Client client, @Inject VarlamoreUimConfig config
       - startUp(): log.debug("Varlamore UIM plugin started")
       - shutDown(): log.debug("Varlamore UIM plugin stopped")
       - @Provides VarlamoreUimConfig provideConfig(ConfigManager configManager) method
       - Do NOT add panel, boundary checker, or event handlers yet -- those come in Plan 02

    3. Create `src/main/java/com/varlamoreuim/VarlamoreUimConfig.java`:
       - Package: `com.varlamoreuim`
       - @ConfigGroup("varlamoreuim")
       - Extends Config
       - Single config item for now: enabled() toggle, default true, keyName "pluginEnabled", name "Enable Plugin", description "Enable or disable Varlamore UIM restrictions"
       - Do NOT add sections or other config items yet -- those come in Plan 02

    4. Create `src/test/java/com/varlamoreuim/VarlamoreUimPluginTest.java`:
       - Package: `com.varlamoreuim`
       - Same pattern as ExamplePluginTest but loading VarlamoreUimPlugin.class
       - ExternalPluginManager.loadBuiltin(VarlamoreUimPlugin.class)

    5. Update `build.gradle`:
       - Change `group = 'com.varlamoreuim'`
       - Change pluginMainClass to `'com.varlamoreuim.VarlamoreUimPluginTest'`

    6. Update `settings.gradle`:
       - Change rootProject.name to `'varlamore-uim'`

    7. Update `runelite-plugin.properties`:
       - displayName=Varlamore UIM
       - author=Sallis
       - description=Varlamore area-lock restrictions for Ultimate Ironman accounts
       - tags=varlamore,uim,ironman,region,restriction
       - plugins=com.varlamoreuim.VarlamoreUimPlugin

    8. Update `src/test/resources/logback-test.xml`:
       - Change logger name from "com.example" to "com.varlamoreuim"

    9. Create `LICENSE` file with BSD-2-Clause license text (required by Plugin Hub). Use current year 2026, copyright holder "Sallis".

    Anti-patterns to avoid:
    - Do NOT leave any com.example references anywhere
    - Do NOT use @ConfigSection yet (Plan 02)
    - Do NOT add ClientToolbar, NavigationButton, or panel imports yet (Plan 02)
  </action>
  <verify>
    Run `./gradlew build` from the project root. Must compile without errors.
    Run `grep -r "com.example" src/` to confirm no old package references remain.
    Verify LICENSE file exists and contains "BSD-2-Clause".
  </verify>
  <done>
    Project compiles as "Varlamore UIM" with com.varlamoreuim package. No com.example references remain. LICENSE file present. runelite-plugin.properties has correct metadata.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BoundaryChecker service and Varlamore region data</name>
  <files>
    src/main/java/com/varlamoreuim/BoundaryChecker.java
    src/main/resources/varlamore_regions.json
  </files>
  <action>
    Create the boundary detection service that determines if a WorldPoint is inside or outside Varlamore.

    1. Create `src/main/resources/varlamore_regions.json`:
       - JSON object with fields: "description" (string), "version" (string "1.0"), "regions" (array of integers), "quetzalWhitelist" (array of integers for internal transport region IDs)
       - For "regions", populate with known Varlamore region IDs. Varlamore spans roughly the southwest quadrant of the OSRS world map. The region IDs can be calculated from the world map coordinates. Based on the OSRS Wiki Varlamore map data, Varlamore encompasses the following approximate region ID ranges:
         - Use placeholder region IDs that cover the known Varlamore area. Include a generous set:
           Region IDs for Varlamore (calculated from world map chunks): 1437, 1438, 1439, 1440, 1441, 1442, 1443, 1444, 1445, 1446, 1447, 1448, 1469, 1470, 1471, 1472, 1473, 1474, 1475, 1476, 1477, 1478, 1479, 1480, 1693, 1694, 1695, 1696, 1697, 1698, 1699, 1700, 1701, 1702, 1703, 1704, 1705, 1706, 1707, 1708, 1709, 1710, 1711, 1712, 1713, 1714, 1715, 1716, 1717, 1718, 1719, 1720, 1721, 1722, 1723, 1724, 1725, 1726, 1727, 1728, 1729, 1730, 1731, 1732, 1733, 1734, 1735, 1736, 1737, 1738, 1739, 1740, 1741, 1742, 1743, 1744, 1745, 1746, 1747, 1748, 1749, 1750, 1751
         - Add a JSON comment field "note": "Region IDs are placeholders. Accurate IDs must be collected in-game using RuneLite world-location plugin by walking the Varlamore boundary. See STATE.md blockers."
       - For "quetzalWhitelist": empty array [] for now (NPC IDs need in-game collection)

    2. Create `src/main/java/com/varlamoreuim/BoundaryChecker.java`:
       - Package: `com.varlamoreuim`
       - @Slf4j annotation
       - Private field: `private final Set<Integer> varlamoreRegions = new HashSet<>()`
       - Private field: `private final Set<Integer> quetzalWhitelistedRegions = new HashSet<>()`
       - Private field: `private boolean loaded = false`

       - Method `public void loadRegions()`:
         - Use getResourceAsStream("/varlamore_regions.json") -- NOT getResource()
         - Parse with Gson (bundled with RuneLite, import com.google.gson.Gson and com.google.gson.JsonObject)
         - Read the "regions" array into varlamoreRegions HashSet
         - Read the "quetzalWhitelist" array into quetzalWhitelistedRegions HashSet
         - Set loaded = true
         - Wrap in try-catch, log.error on failure with descriptive message
         - log.debug("Loaded {} Varlamore regions", varlamoreRegions.size()) on success

       - Method `public boolean isInVarlamore(WorldPoint location)`:
         - Return varlamoreRegions.contains(location.getRegionID())
         - This is O(1) HashSet lookup per BNDRY-02

       - Method `public boolean isInVarlamore(int regionId)`:
         - Overloaded version accepting raw region ID
         - Return varlamoreRegions.contains(regionId)

       - Method `public boolean isQuetzalWhitelisted(int regionId)`:
         - Return quetzalWhitelistedRegions.contains(regionId)
         - Supports BNDRY-03 (internal transport whitelisting)

       - Method `public boolean isLoaded()`:
         - Return loaded flag

       - Method `public int getRegionCount()`:
         - Return varlamoreRegions.size()

       Import WorldPoint from net.runelite.api.coords.WorldPoint.

    Anti-patterns to avoid:
    - Do NOT use ArrayList for region storage (O(n) lookup)
    - Do NOT use getResource() -- must use getResourceAsStream() for JAR compatibility
    - Do NOT log at INFO level for routine operations -- use DEBUG
    - Do NOT hardcode region IDs in Java -- load from JSON resource file
  </action>
  <verify>
    Run `./gradlew build` from project root. Must compile without errors.
    Verify varlamore_regions.json exists in src/main/resources/.
    Verify BoundaryChecker.java uses HashSet (not ArrayList) and getResourceAsStream (not getResource).
  </verify>
  <done>
    BoundaryChecker loads region IDs from JSON into HashSet, provides O(1) isInVarlamore() lookup, and has Quetzal whitelisting support. Project compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- `./gradlew build` compiles without errors
- No com.example references remain in source tree
- BoundaryChecker uses HashSet for O(1) region lookup
- Region data loaded via getResourceAsStream() (JAR-safe)
- LICENSE file contains BSD-2-Clause
- runelite-plugin.properties references com.varlamoreuim.VarlamoreUimPlugin
</verification>

<success_criteria>
- Plugin project identity is fully transformed from example to Varlamore UIM
- BoundaryChecker service exists with O(1) region lookup capability
- Region data externalized to JSON resource file
- Quetzal whitelist infrastructure ready for population
- Full project compiles with `./gradlew build`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md`
</output>
